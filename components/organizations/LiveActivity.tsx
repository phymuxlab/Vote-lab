import { Activity } from "lucide-react";
interface ActivityItem { id: string; created_at: string; nominees?: { full_name: string | null }[]; }
interface Props { activity: ActivityItem[]; }
export default function LiveActivity({ activity }: Props) { return <section className="rounded-3xl border border-white/8 bg-white/[0.035] p-5 sm:p-6"><div className="mb-5 flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-slate-500">Realtime</p><h2 className="mt-1 text-xl font-semibold text-white">Live activity</h2></div><span className="flex items-center gap-2 rounded-full bg-emerald-400/10 px-2.5 py-1 text-[11px] font-semibold text-emerald-300"><span className="h-1.5 w-1.5 animate-pulse rounded-full bg-emerald-300" /> Live</span></div>{activity.length === 0 ? <div className="rounded-2xl border border-dashed border-white/8 p-5 text-sm text-slate-500">No activity yet.</div> : <div className="space-y-2">{activity.map((vote) => <div key={vote.id} className="flex gap-3 rounded-2xl border border-white/6 bg-slate-950/40 p-4"><div className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-cyan-400/10 text-cyan-300"><Activity className="h-4 w-4" /></div><div><p className="text-sm font-semibold text-white">Vote recorded</p><p className="mt-1 text-xs text-slate-500">{vote.nominees?.[0]?.full_name ?? "Unknown nominee"} · {new Date(vote.created_at).toLocaleString()}</p></div></div>)}</div>}</section>; }
