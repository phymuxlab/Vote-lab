import Image from "next/image";
import { Vote } from "lucide-react";

interface ElectionHeroProps { title: string; description: string | null; organizationName: string; logo?: string | null; }
export default function ElectionHero({ title, description, organizationName, logo }: ElectionHeroProps) { return <section className="text-center">{logo ? <div className="mb-6 flex justify-center"><Image src={logo} alt={`${organizationName} logo`} width={100} height={100} className="rounded-[1.75rem] border border-cyan-400/30 bg-white object-cover shadow-xl shadow-cyan-500/10" /></div> : <div className="mx-auto mb-6 flex h-24 w-24 items-center justify-center rounded-[1.75rem] border border-cyan-400/20 bg-cyan-400/10 text-cyan-300"><Vote className="h-10 w-10" aria-hidden="true" /></div>}<p className="text-sm font-semibold uppercase tracking-[0.35em] text-cyan-400">Powered by Vote Lab</p><h1 className="mt-6 text-4xl font-extrabold tracking-tight text-white sm:text-5xl">{title}</h1><p className="mt-3 text-lg font-medium text-cyan-400">{organizationName}</p>{description && <p className="mx-auto mt-6 max-w-3xl text-base leading-8 text-slate-400 sm:text-lg">{description}</p>}</section>; }
