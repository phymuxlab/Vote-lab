import { CheckCircle2, Vote } from "lucide-react";
interface RecentVote { id: string; created_at: string; }
interface RecentVotesProps { votes: RecentVote[]; }
export default function RecentVotes({ votes }: RecentVotesProps) { return <section className="rounded-3xl border border-white/8 bg-white/[0.035] p-5 sm:p-6"><div className="mb-5 flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-slate-500">Activity</p><h2 className="mt-1 text-xl font-semibold text-white">Recent votes</h2></div><span className="inline-flex items-center gap-2 rounded-full bg-emerald-400/10 px-2.5 py-1 text-[11px] font-semibold text-emerald-300"><span className="h-1.5 w-1.5 rounded-full bg-emerald-300" /> Live</span></div>{votes.length === 0 ? <p className="text-sm text-slate-500">No voting activity yet.</p> : <div className="grid gap-2 sm:grid-cols-2">{votes.map((vote) => <div key={vote.id} className="flex items-center gap-3 rounded-2xl border border-white/6 bg-slate-950/35 p-4"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-cyan-400/10 text-cyan-300"><Vote className="h-4 w-4" /></div><div className="min-w-0 flex-1"><p className="text-sm font-semibold text-white">Vote recorded</p><p className="mt-1 text-xs text-slate-500">{new Date(vote.created_at).toLocaleString()}</p></div><CheckCircle2 className="h-4 w-4 text-emerald-300" /></div>)}</div>}</section>; }
